<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Items

    <a data-toggle="collapse" href="#status_items_new_form">
      Add item
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @status.items.each do |item| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to item, item %>

        <% if current_user == item.owner %>
          <%= link_to item, method: :delete do %>
            <i class="fas fa-trash-alt"></i>
          <% end %>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="status_items_new_form">
        <%= render partial: "statuses/status_items_new_form" %>
      </div>
    </div>

  </ul>
</div>
